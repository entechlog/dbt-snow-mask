version: 2

sources:
  - name: seeds
    tables:
      - name: customer
        columns:
          - name: first_name
            meta:
                masking_policy: mp_encrypt_pii
          - name: create_date
            meta: 
                masking_policy: mp_mask_timestamp
